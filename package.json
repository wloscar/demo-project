{
  "name": "bi-component",
  "version": "1.0.0",
  "author": "",
  "description": "",
  "scripts": {
    "copy": "cross-env cp package.json build/package.json",
    "zip": "cross-env cd build && zip -q -r component.zip main.css main.js package.json&& cd ..",
    "pack": "cross-env rm -rf build && npm run build && npm run copy && npm run zip",
    "start": "cross-env qbi start",
    "build": "cross-env qbi build"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/echarts": "^4.6.5",
    "cross-env": "^7.0.3",
    "qbi-dev-tools": "^0.1.11",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "bi-open-sdk": "^1.0.0-qbi310.65",
    "echarts": "^4.8.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
